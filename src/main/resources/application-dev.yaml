spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lumina?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Zh@123123
  redis:
    host: localhost
    port: 6379
    database: 0

logging:
  level:
    com.lumina: DEBUG
    com.lumina.mapper: INFO
    com.baomidou.mybatisplus: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO

lumina:
  rate-limit:
    enabled: false
  auth:
    jwt:
      expiration: 604800000  # 7天 for development